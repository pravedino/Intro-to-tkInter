# grid() sticky
name_label = Label(root, text="Name:", bg="grey")
password_label = Label(root, text="Password:", bg="grey")
name_label.grid(row=0, column=0, sticky=E)
password_label.grid(row=1, column=0)
